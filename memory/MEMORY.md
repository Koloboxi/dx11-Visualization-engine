# Memory index

- [SEM DLL cache semantics](sem-dll-cache-semantics.md) — only SEM_LoadCSV3D clears the cache; AddFromCSV3D is a pure visual load; subdivide drops offsets/mesh so recompute offsets→mesh.
