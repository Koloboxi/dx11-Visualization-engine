# Memory index

- [SEM DLL cache semantics](sem-dll-cache-semantics.md) — only SEM_LoadCSV3D clears the cache; AddFromCSV3D is a pure visual load; subdivide drops offsets/mesh so recompute offsets→mesh. Pipeline lives in SemSession, auto-branching 2D contour / 3D surface on import.
- [SEM staging selection](sem-staging-selection.md) — Primitive::staging is a second-level selection (not the gizmo); double-click tree row to stage, double-click 3D space to clear; SEM panel binds to it.
